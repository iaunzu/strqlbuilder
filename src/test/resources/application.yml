spring:
    jpa:
        properties:
            hibernate:
                format_sql: true
        hibernate:
            ddl-auto: update

logging:
    level:
        org.hibernate.SQL: DEBUG
        org.hibernate.type.descriptor.sql.BasicBinder: TRACE
